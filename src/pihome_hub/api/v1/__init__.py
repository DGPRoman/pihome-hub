"""Version 1 of the home automation API.

Relay control and sensor ingestion routers land here, behind API-key authentication.
Everything in this package carries a compatibility promise: a breaking change gets a
new version prefix rather than a silent edit to ``/v1``.
"""
